import 'package:flutter/material.dart';

const EdgeInsets stockPadding = EdgeInsets.all(8.0);
const EdgeInsets criteriasPadding =
    EdgeInsets.only(left: 16.0, right: 16.0, top: 32.0, bottom: 8.0);
const EdgeInsets criteriaPadding = EdgeInsets.only(bottom: 12.0);
const indicatorPadding =
    EdgeInsets.only(left: 20.0, right: 20.0, top: 32.0, bottom: 8.0);
const periodPadding = EdgeInsets.all(16.0);
const valuesPadding =
    EdgeInsets.only(left: 32.0, right: 32.0, top: 24.0, bottom: 8.0);
const valuePadding = EdgeInsets.all(16.0);

const periodMargin = EdgeInsets.only(top: 12.0);
